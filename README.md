# HelpDesk Backend API

Support Ticket System Backend

## Tech Stack
- Node.js + Express.js
- MongoDB + Mongoose
- JWT Authentication

## Setup
1. Clone the repo
2. Run `npm install`
3. Create `.env` file
4. Run `npm run dev`

## API Base URL
`http://localhost:5000/api`